language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

install:
  - pip install nose coveralls
  - pip install .

script: 
    - nosetests -v --exe --with-cov --cover-package pyre

after_success:
    - coveralls
